<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.simeplui">

    <!-- 告知用戶使用的權限 -->
    <!-- 使用網路權限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 使用網路狀態權限 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 使用網路狀態權限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!--  -->
    <application
        android:name=".SimpleUIApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- 參數設定 -->
        <meta-data
            android:name="com.parse.APPLICATION_ID"
            android:value="LSGC6MPAWP63QiDHQaLxCY8KCmjaW5tZZoPp1GoL" />
        <meta-data
            android:name="com.parse.CLIENT_KEY"
            android:value="wnZqlP6AbsRJPq1XoZUDI1BKFfUZJghHbUc9n0s1" />
        <!-- 參數設定 -->

        <activity android:name=".MainActivity">
            //主程式程式開始地方
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DrinkMenuActivity"
            android:label="@string/title_activity_drink_menu"
            android:theme="@style/AppTheme" />
            //主程式程式開始地方(第二個頁面)
        <activity
            android:name=".OrderDetailActivity"
            android:label="@string/title_activity_order_detail"
            android:theme="@style/AppTheme">
        </activity>
    </application>

</manifest>
